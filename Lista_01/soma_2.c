#include <stdio.h>
#include <stdlib.h>

int main(void){
    int x;
    int y;
    int z;

    scanf("%d", &x);
    scanf("%d", &y);

    z = x + y;

    printf("%d\n", z);

    return 0;
}